version: 2
sources:
    - name: datafeed_shared_schema
      database: "{{('DEMO_SALES')}}"
      schema: raw_schema
      tables:
        - name: raw_customerdata
        - name : raw_ordersdata
        - name : customers
        
        
